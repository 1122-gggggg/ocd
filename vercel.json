{
  "//": "migrate-in-build: Vercel has no pre-start hook, so `prisma migrate deploy` runs in buildCommand to ensure the Neon (ap-northeast-1 Tokyo) schema is current before the new deployment serves traffic. Regions hnd1 keeps serverless functions next to Neon Tokyo.",
  "framework": "nextjs",
  "regions": ["hnd1"],
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npm run build",
  "installCommand": "npm ci",
  "functions": {
    "src/app/api/auth/[...nextauth]/route.ts": {
      "maxDuration": 10
    },
    "src/app/api/cron/backup/route.ts": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron/backup",
      "schedule": "0 3 * * *"
    }
  ]
}
